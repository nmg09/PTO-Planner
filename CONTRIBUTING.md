# Contributing

## Branching

- Keep `main` deployable.
- Create feature branches: `feature/<name>`.
- Merge via PR only.

## Local checks

Run before opening a PR:

```bash
npm run typecheck
npm run test
npm run build
```
